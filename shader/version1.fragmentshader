#version 330 core

// Input data
in vec2 xy;

// Ouput data
out vec3 color;

void main()
{
    // Calculate view angle
    float c = sqrt(xy.x * xy.x + xy.y * xy.y);

    // Output color
	color = vec3(0, c, c);

}